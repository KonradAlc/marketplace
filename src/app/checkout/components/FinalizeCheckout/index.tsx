import React from "react";

type FinalizeCheckoutProps = {};

const FinalizeCheckout = (props: FinalizeCheckoutProps) => {
  return (
    <div>
      <h2>Podsumowanie</h2>
    </div>
  );
};

export default FinalizeCheckout;
